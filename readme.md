# MyRestaurant

Angular SSR + NestJS starter.

## Structure

- `app/src`: Angular frontend
- `app/api`: NestJS backend

## Run

1. Install dependencies from the repo root with `npm install`.
2. Start the frontend with `cd app | ng serve`.
