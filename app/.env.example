# Copy this file to .env and fill in your own value. .env is gitignored and never committed.
#
# Get a key at https://console.cloud.google.com/google/maps-apis/credentials
# Enable the "Places API (New)" on it, and restrict it by HTTP referrer to your domain(s).
GOOGLE_MAPS_API_KEY=
